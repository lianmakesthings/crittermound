language: node_js
sudo: true
before_install:
  - sudo apt-get update -q
  - npm cache clean -f
  - npm install -g n
  - sudo n stable
install: npm install
script: node build/build.js
deploy:
  provider: pages
  github-token: $GITHUB_TOKEN
  local-dir: dist