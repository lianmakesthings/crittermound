language: node_js
sudo: true
before_install:
  - sudo apt-get update -q
  - nvm install node
  - nvm use node
  - npm install npm -g
install: npm install
script: node build/build.js
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: dist